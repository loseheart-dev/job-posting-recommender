# D2 日报（25051430 徐挚凌）

## 昨日遗留问题的回答
- D1 遗留的"接口字段不一致、JobRecord 位置参数"两个问题已在上一步 review 中修复并通过；D1 末尾的"实现网站配置与采集任务调度服务"即今日目标

## 今日目标
- 实现网站配置服务与采集任务调度服务
- 将服务层测试改写为 unittest，同步 main 并解决冲突

## 实际进展
- 实现网站配置服务 `site_service.py`（增删改查、启停、频率校验）
- 实现采集任务调度服务 `task_service.py`（手动触发、状态/记录查询、下次计划时间纯计算）
- 将 `test_site_task.py` 改写为 unittest，`unittest discover` 可自动发现
- 同步最新 main 并解决 `schema.py`/`smoke.py` 合并冲突
- 按组长 review 意见修复：`experience` 兼容字段、`site_name`/`base_url` 必填、任务计数校验

## 遇到的问题与解决
- 个人分支与 main 存在合并冲突（共享接口文件两处改动）→ 以 main 优先解决
- 组长提出 `experience` 需保留兼容而非拒绝、`site_name`/`base_url` 需必填校验、任务计数不能为负 → 按意见补齐校验

## 明日计划
- 数据服务接入真实清洗数据、算法调用适配

