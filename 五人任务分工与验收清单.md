# 五人任务分工与验收清单

## 项目信息

- 项目名称：`面向大学生求职的岗位数据分析与个性化推荐系统`
- 组长：`[待确认]`
- 成员：`卢世豪`、`丁伟哲`、`郑维豪`、`徐挚凌`、`王宇杰`
- 指定验收人：组长 + 对应任务的另一名成员

姓名和组长确定后，只替换对应占位内容，不改变验收边界。

## 总体交付目标

项目最终必须能够完成一条完整链路：

```text
数据导入/采集 → 数据清洗 → 算法分析 → 结果可视化 → 用户操作得到结果
```

每名成员都要有一个可以独立演示 1—2 分钟的成果。只负责“协助”“查资料”或“调用 AI”不算独立交付物。

## 任务分工

每个人只修改自己的任务目录。需要改共享接口时，先在群里确认字段和函数签名，再提交代码。

### 卢世豪：算法统筹，负责 KMeans 聚类和随机森林薪资预测

主要任务：把清洗后的岗位数据转换成可用于建模的特征，完成岗位聚类、薪资预测和模型评价，并向后端提供可调用函数。

下一步小步骤：

1. 从郑维豪处拿到清洗后数据样例，确认岗位类别、技能、地区、经验、教育和薪资字段；
2. 在 `src/algorithms/clustering.py` 中完成特征选择、标准化、KMeans 训练，输出每条岗位的群组标签和每个群组的特征统计；
3. 在 `src/algorithms/salary.py` 中完成训练集/测试集划分、随机森林回归和 MAE 或 R² 计算；
4. 用正常数据、缺字段数据各运行一次，把函数输入、输出和异常提示交给徐挚凌；
5. 给王宇杰提供聚类图、薪资预测结果和指标字段，完成一次页面联调。

交付物：`clustering.py`、`salary.py`、特征说明、样例结果、指标记录和调用说明。

验收标准：聚类结果包含群组解释；薪资预测使用独立测试数据；有 MAE 或 R²；数据量不足或字段缺失时有明确提示；徐挚凌能调用，王宇杰能展示。

### 丁伟哲：算法成员，负责 TF-IDF 个性化岗位推荐

主要任务：根据学生画像和岗位文本计算匹配度，返回 Top-K 岗位，并说明匹配技能、缺失技能和推荐理由。

下一步小步骤：

1. 和郑维豪确认技能字段的格式、分隔符、大小写和空值标记；
2. 和徐挚凌确认画像字段：目标岗位、技能、地区、经验和薪资偏好；
3. 在 `src/algorithms/recommender.py` 中合并岗位标题、技能和描述，完成 TF-IDF 向量化与余弦相似度排序；
4. 增加匹配技能、缺失技能和理由字段，准备技术岗、数据岗、无技能三组画像；
5. 测试空岗位、空技能、缺失描述和筛选无结果情况，把稳定返回格式交给徐挚凌和王宇杰。

交付物：`recommender.py`、画像字段说明、3 组测试画像、Top-K 结果和推荐解释。

验收标准：画像变化会改变排序或解释；结果不是写死的；每条推荐包含岗位、分数、匹配技能和缺失技能；无结果时有提示；徐挚凌能调用，王宇杰能展示。

### 郑维豪：后端成员，负责数据采集、清洗和数据管理

主要任务：把公开岗位数据整理成算法和页面都能使用的统一 CSV，保留原始数据、字段说明和处理记录。

下一步小步骤：

1. 确认公开数据集来源和使用范围，把原始文件放在 `data/raw/`，不覆盖、不修改原文件；
2. 统计原始数据的字段、记录数、重复值、空值和薪资格式，补充数据字典；
3. 在 `src/data/cleaner.py` 中完成去重、缺失值处理、薪资统一、地区和经验字段标准化；
4. 输出 `data/processed/jobs.csv`，同时记录清洗前后记录数和异常处理方式；
5. 用正常 CSV、空文件、缺字段文件各运行一次，把字段名、路径和缺失规则交给卢世豪、丁伟哲、徐挚凌。

交付物：原始数据说明、`cleaner.py`、清洗后 `jobs.csv`、数据字典和处理记录。

验收标准：原始文件不被覆盖；脚本可以重复运行；清洗前后记录数和缺失值可说明；空文件、缺字段和坏格式有提示；算法和后端使用同一份字段约定。

### 徐挚凌：后端成员，负责数据服务和前后端联调

主要任务：把数据读取、岗位筛选、统计和三个算法的调用封装成页面可以直接使用的服务函数，负责跑通完整链路。

下一步小步骤：

1. 根据郑维豪的数据字典，在 `src/data/loader.py` 中固定数据路径、读取方式和缺文件提示；
2. 在 `src/services/job_service.py` 中完成岗位关键词、地区、经验、工作方式和薪资筛选；
3. 增加岗位数量、技能频率、薪资分布等统计函数，统一返回字段和空结果状态；
4. 接入卢世豪、丁伟哲的算法函数，保证页面不直接读取原始数据，也不重复写模型逻辑；
5. 依据王宇杰的页面调用需求完成联调，补充 README 启动步骤和一条可重复演示路径。

交付物：数据读取模块、服务模块、调用示例、联调记录和 README 启动说明。

验收标准：前端通过服务函数拿到数据；筛选条件会影响统计和算法输入；模型未加载、文件缺失和筛选无结果时有明确状态；项目可从头启动并走通一条演示链路。

### 王宇杰：前端成员，负责 Streamlit 页面和可视化

主要任务：把岗位检索、市场统计、算法结果和个性化推荐做成可操作页面，确保输入变化能反映到结果。

下一步小步骤：

1. 在 `app.py` 和 `src/ui/` 中搭好页面布局，先放置岗位检索、数据仪表盘、算法分析、个性化推荐四个区域；
2. 接入徐挚凌的数据读取和筛选函数，完成关键词、地区、经验和薪资控件；
3. 接入统计、聚类、薪资预测和推荐结果，分别绘制岗位数量、技能频率、薪资分布和聚类结果；
4. 做推荐结果卡片，显示岗位名称、匹配分数、匹配技能、缺失技能和推荐理由；
5. 用正常数据、筛选无结果、文件缺失和模型未加载状态测试页面，并保留截图和演示路径。

交付物：`app.py`、`src/ui/` 页面代码、交互控件、图表、推荐展示和演示截图。

验收标准：一条命令可以启动；输入变化会改变表格、图表或推荐排序；图表有标题和图例；页面不写死结果；空数据和模型未加载时有提示。

> 组长从五人中确认一人，负责 README、合并、进度和最终演示，但不能替代其他成员的独立交付。

## 每个人的提交清单

提交前逐项确认：

- [ ] 我完成了任务卡中的代码或文档；
- [ ] 我在本地运行过至少一个正常案例；
- [ ] 我验证过一个异常或空数据案例；
- [ ] 我看过 `git diff`，没有无关改动；
- [ ] 我写清楚了运行命令和验证结果；
- [ ] 我能在答辩中解释自己的核心代码；
- [ ] 我使用 AI 生成的部分已经实际检查，不是直接照搬。

## 合并前清单

组长合并前确认：

- [ ] 五人的分支都有有效提交；
- [ ] 数据来源、字段和算法名称已补齐；
- [ ] 项目可以从头启动并走通一条演示链路；
- [ ] 三类算法都有实际输入、输出和评价/解释；
- [ ] 页面有异常提示，不能运行的功能已明确标注；
- [ ] README、演示截图和任务完成状态一致；
- [ ] 没有密钥、Cookie、个人信息和无关大文件。

## 简单进度节奏

| 时间 | 全组目标 |
|---|---|
| 第一天 | 定题、定数据、定三类算法，完成页面草图和分工确认 |
| 第二天 | 数据清洗可运行，页面骨架可启动，算法输入输出定稿 |
| 第三天 | KMeans、随机森林、TF-IDF 推荐分别跑通一个样例，完成基础图表 |
| 第四天 | 模块集成，补充评价指标和异常处理，准备中期检查 |
| 第五天 | 完成联调、演示流程、README 和个人成果记录 |
| 第六天 | 全流程验收、录制演示、修复阻塞问题、整理报告 |

## 阻塞记录

| 日期 | 成员 | 问题 | 已尝试 | 需要的帮助 | 状态 |
|---|---|---|---|---|---|
| `[日期]` | `[姓名]` | `[具体问题]` | `[已尝试内容]` | `[需要谁协助]` | `[待处理/已解决]` |
