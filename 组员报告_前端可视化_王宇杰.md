# 组员报告：前端开发与可视化——王宇杰

## 一、整个项目是什么

项目采集在线招聘平台岗位数据，面向大学生实习和校招提供岗位检索、岗位市场分析、算法结果展示和个性化推荐。页面不是静态 PPT：用户改变筛选条件或学生画像后，必须调用后端和算法模块得到真实结果。

完整链路为“郑维豪提供清洗数据 → 徐挚凌提供服务函数 → 卢世豪完成聚类与薪资预测、丁伟哲完成推荐 → 我负责页面输入、图表和结果展示”。技术栈是 Python、Streamlit、pandas 和 scikit-learn。

## 二、我的职责

- 使用 Streamlit 完成一个可运行的 Web 页面；
- 完成岗位检索、数据仪表盘、算法结果、个性化推荐四个功能区；
- 对接徐挚凌的数据读取、筛选、统计和模型调用函数；
- 展示岗位数量、技能热度、薪资分布、地区/经验差异和推荐结果；
- 给推荐卡片增加匹配技能、缺失技能和主要匹配因素；
- 处理空数据、无结果、模型未加载和输入错误状态。

我不负责在页面中重新实现算法，不在代码里写死图表数据，不为了视觉效果隐藏错误。

## 三、我的交付物

1. Streamlit 页面代码；
2. 四个功能区的输入控件和结果展示；
3. 至少三种有意义的可视化图表；
4. 一条从筛选到推荐的完整演示路径；
5. 正常数据和空/异常数据截图；
6. 页面启动说明；
7. 一分钟讲解稿、AI Agent 关键提示词和验证记录。

## 四、验收标准

- 一条命令可以启动页面；
- 至少有岗位检索、数据仪表盘、算法结果、个性化推荐四个功能区；
- 筛选条件变化会改变表格、统计或图表；
- 图表有标题、图例、单位或数据范围说明；
- 推荐结果显示岗位、分数、匹配技能、缺失技能和推荐理由；
- 数据为空、筛选无结果、模型未加载时页面给出可理解提示；
- 页面不直接读原始数据，不硬编码最终结果；
- 我本人能解释每个控件如何调用后端、每张图表达什么。

## 五、交给 AI Agent 的任务单

```text
目标：使用现有 Python 技术栈完成岗位分析与推荐 Streamlit 页面。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、徐挚凌提供的服务函数和真实返回字段。
输入：岗位筛选条件、统计结果、三类算法结果和推荐结果。
输出：四个功能区、至少三种图表、异常提示、启动说明和演示截图。
限制：不得在页面硬编码岗位数据或模型结果；不得重新实现算法；不得新增无必要前端框架；不得隐藏错误。
验收：一条命令启动；筛选和画像输入会影响结果；空数据和模型缺失有提示；图表标题和图例完整；列出修改文件和验证步骤。
```

AI Agent 修改后先看 diff，再逐项点击页面控件；不要只确认页面“能打开”，必须确认输入确实改变了输出。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责前端和可视化。页面分为岗位检索、数据仪表盘、算法结果和个性化推荐四个区域。用户可以先按地区、经验、薪资等条件筛选岗位，再填写技能和求职偏好获得推荐。页面中的数据和图表都由后端服务实时返回，推荐卡片还会展示匹配技能和缺失技能，保证结果可解释。对于无数据和模型未加载等情况，页面会给出提示。”

### 可能被问的问题

**问：页面如何证明不是静态图？**

答：现场修改地区、岗位关键词或学生技能，表格、图表或推荐排序会跟着变化；页面不保存一套写死的最终结果。

**问：为什么选 Streamlit？**

答：课程目标是快速完成 Python 数据分析 Web 原型，Streamlit 能直接承载控件、表格和图表，减少前端工程量，把时间用于数据和算法解释。

**问：页面最重要的可视化是什么？**

答：技能热度帮助学生了解市场需求，薪资分布帮助形成预期，地区/经验对比帮助筛选方向；每张图都对应一个求职问题，而不是为了凑图表数量。

## 七、协作接口

- 向徐挚凌索取稳定函数和返回字段，不自行猜测字段名；
- 向郑维豪索取数据字典，用于页面标签、单位和说明；
- 向卢世豪、丁伟哲索取聚类、预测和推荐样例；
- 发现后端返回不一致时先记录错误，不在页面里偷偷兼容多套格式；
- 完成后给组长一条可重复演示路径。

## 八、不要做的事

不要先做复杂登录、权限、响应式大屏或动画。明天答辩最重要的是四个功能区能跑通、输入能影响结果、图表说得清楚；这些完成后再考虑美化。

## 九、下一步小步骤

1. 今天先在 `app.py` 和 `src/ui/` 中搭好四个页面功能区；
2. 接入徐挚凌的数据读取和筛选函数，完成关键词、地区、经验和薪资控件；
3. 接入统计、聚类、薪资预测和推荐结果，给每张图补充标题、图例和单位；
4. 制作推荐结果卡片，显示岗位、匹配分数、匹配技能、缺失技能和推荐理由；
5. 用正常数据、无筛选结果、文件缺失和模型未加载四种状态测试页面，保留截图和演示步骤。
