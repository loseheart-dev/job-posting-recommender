# D4 明日计划终态对账

对账日期：2026-09-05
对账基线：`origin/main`（`6d9c4ad`）

| D4 明日计划 | 对账结果 | 依据 |
|---|---|---|
| 合并 PR #26 | 已完成 | PR #26 已于 2026-09-04 合并，提交 `73cf690` 已进入主线 |
| 前端验收 | 基础集中联调已完成，完整 R6 仍未闭环 | PR #36 已合并；`tests/verify_app_integration.py` 已进入主线；`todos.md` 中 R6 仍为未完成 |
| 复查 main 薪资口径 | 本分支已完成，待 PR 合并 | `AGENTS.md`、`docs/岗位数据字典.md`、`tests/verify_salary_real_data.py` 已补充终态规则和验证结果 |

## 当前可复算结果

基于本机 `data/processed/jobs.csv` 的 8608 条重洗结果：7834 条有效月薪，774 条非月薪或不可解析薪资被置空，19 条 10 万元及以上月薪保留；薪资分布 52 桶，随机森林 MAE=4970.88 元，R²=0.1937。详细分桶和断言结果见 `tests/fixtures/real_data_salary_verification.json`。

D4 中记录的 R²=0.234 属于旧的高值阈值中间方案，作为历史对照保留；当前终态不恢复“超过 10 万元即删除”的规则。本日后续计划只保留未完成项：提交本分支改动供审核、完成 R6 页面验收，并进行最终完整链路演示。
