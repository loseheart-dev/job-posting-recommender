# D1 日报（25051430 徐挚凌）

## 今日目标
- 完成仓库认证、阅读项目规范，确定个人角色与模块
- 完成共享接口对齐（岗位表、筛选键、学生画像、必填字段）

## 实际进展
- 完成 GitHub 认证与仓库访问确认，克隆仓库并阅读项目规范（AGENTS.md、README、《接口约定》、《五人任务分工与验收清单》）
- 确定个人角色与模块：后端成员，负责采集管理、调度、数据服务和前后端联调
- 完成共享接口对齐：`JOB_COLUMNS` 补齐 21 列、`FILTER_KEYS` 补齐 9 个、`StudentProfile` 补齐 11 个字段（含 experience 兼容字段）、`JobRecord` 保留位置参数、必填字段补 `source`/`crawled_at`
- 更新 `job_service.py` 筛选逻辑，补充服务契约冒烟测试并通过

## 遇到的问题与解决
- `src/data/schema.py` 与《接口约定》字段不一致 → 逐一核对并补齐
- `JobRecord` 字段顺序调整破坏了原有位置参数调用（review 反馈）→ 恢复 `skills` 为第 3 位置参数

## 明日计划
- 实现网站配置与采集任务调度服务
