# 组员报告：后端数据采集、清洗与管理——郑维豪

## 一、整个项目是什么

项目为大学生提供岗位市场分析和个性化推荐，在线招聘平台岗位数据采集是系统的数据入口。系统的可信度首先取决于数据：如果采集范围不清、岗位字段混乱、薪资单位不一致、重复岗位很多，后面的聚类、预测和推荐都会失真。因此我的任务是从智联招聘、前程无忧（51job）等平台采集大数据相关岗位，并把采集结果整理成算法和页面都能稳定使用的清洗数据。

全流程为“在线招聘平台 → 我的岗位采集与清洗 → 徐挚凌数据服务 → 两位算法成员处理 → 王宇杰前端展示”。核心算法为 KMeans、随机森林回归、TF-IDF 推荐。

## 二、我的职责

- 确认数据来源、下载地址、数据许可或使用范围；
- 确定首期采集平台、岗位关键词、城市范围和采集时间；
- 完成智联招聘、51job 的岗位信息采集，保存原始结果和采集记录；
- 实现深度优先和广度优先两种网页遍历策略，保留自研遍历代码和运行记录；
- 按网站分别完成页面字段解析，并统一映射到岗位数据字段；
- 记录岗位数据的字段含义；
- 完成 CSV 导入、字段统一、去重、缺失值处理和薪资标准化；
- 保留原始数据，不覆盖、不混入个人信息和账号 Cookie；
- 输出清洗后的样例数据、数据字典和处理日志；
- 向两位算法成员说明哪些字段可用、哪些字段存在缺失。

我不负责修改算法逻辑和前端样式。算法发现字段问题时，我先定位数据证据，再决定是否调整清洗规则，不让 AI Agent 擅自猜测。

## 三、我的交付物

1. 智联招聘、51job 的平台和采集范围说明；
2. 采集脚本、网站适配代码和可重复的采集运行说明；
3. BFS/DFS 遍历代码、策略选择说明和运行记录；
4. 原始数据和采集记录；
5. 数据字典：字段名、类型、含义、缺失情况；
6. 可重复运行的数据导入/清洗脚本；
7. 清洗前后记录数、重复数、缺失值和异常值统计；
8. 清洗后的可供开发使用的小样本；
9. 一个正常输入和一个错误/空数据案例；
10. 给算法和数据服务成员的输入说明、截图和运行记录。

## 四、验收标准

- 智联招聘和 51job 均有可复现的岗位采集结果，能够说明平台、关键词/城市范围和采集时间；
- 采集结果确实来自在线招聘平台，不能用手工编造的岗位记录替代；
- 采集过程不绕过登录、验证码或访问限制，不保存个人联系方式、Cookie 和账号凭据；
- 相同输入重复运行，清洗结果基本一致；
- 岗位标题、岗位描述/技能、地区、经验和薪资等核心字段有明确类型；
- 重复岗位、缺失薪资、空描述、异常薪资有明确处理或标记；
- 空文件、缺少必要字段、错误格式会输出人能看懂的提示；
- 清洗结果能被算法成员直接读取，不需要手工复制粘贴；
- 数据来源和限制能在答辩中说明；
- 不提交个人信息、Cookie、密钥或未经确认的大型原始文件。

## 五、交给 AI Agent 的任务单

```text
目标：完成在线招聘平台岗位数据的采集、导入、清洗和字段统计。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、现有目录和真实数据样例。
输入：选定平台的岗位页面或公开岗位信息，先检查字段，不得假设不存在的列。
输出：采集说明或脚本、原始数据、清洗脚本、清洗后样例、数据字典、处理日志和清洗前后统计。
限制：先完成智联招聘和 51job 的最小采集范围；不得绕过登录、验证码或访问限制；不得采集个人联系方式；不得覆盖原始数据；不得上传数据到外部服务；不得把缺失值静默删除；不得新增无必要依赖；不要修改算法和前端文件。
验收：两个平台均能采集岗位信息；BFS 和 DFS 均能运行并有结果记录；正常数据可清洗；空文件、缺字段、坏格式有提示；重复运行结果一致；列出平台、采集范围、修改文件和验证命令。
```

AI Agent 修改后先查看 diff，确认只动数据任务范围；再用正常文件和空/错文件各跑一次。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责在线岗位数据采集、清洗和管理。项目从智联招聘、51job 等平台采集大数据相关岗位信息，并按 BFS/DFS 策略完成页面遍历，再统一字段、去重、处理缺失值和薪资格式，保留原始数据与采集处理记录。这样算法成员可以使用稳定的数据，前端也能显示清洗前后的统计。对于无法确定的字段或缺失值，我会保留标记并在数据字典中说明，不伪造数据。”

### 可能被问的问题

**问：为什么需要实现 BFS 和 DFS 两种策略？**

答：BFS 适合按层访问列表页和详情页，DFS 适合沿链接深入访问。实现两种策略可以比较不同遍历方式，并满足采集模块的自研算法要求。首期控制每个平台的关键词和城市范围，保证采集结果可复现；采集过程遵守平台规则，不绕过登录、验证码和访问限制。

**问：缺失薪资怎么处理？**

答：统计分析保留缺失标记；薪资预测只使用有薪资标签的样本，并在页面说明样本范围，不能把缺失值随意当成 0。

**问：怎么证明清洗有效？**

答：展示清洗前后记录数、重复数、缺失值统计和字段类型，并提供同一输入重复运行结果一致的记录。

## 七、协作接口

- 给卢世豪和丁伟哲：清洗后文件路径、字段说明、缺失值规则；
- 给徐挚凌：标准化字段名和数据读取约定；
- 给王宇杰：清洗前后统计，作为数据质量页面素材；
- 发现阻塞时记录“已尝试、具体错误、需要谁协助”。

## 八、不要做的事

不要为了让模型有结果而批量填补虚假薪资，不要覆盖原始文件，不要顺便重构项目目录。数据清洗的目标是可追溯和可复现，不是把所有缺失都“变完整”。

## 九、下一步小步骤

1. 今天确定智联招聘和 51job 的岗位关键词、城市范围、采集时间和使用规则；
2. 完成 BFS/DFS 遍历和两个网站的最小采集，保留原始结果、来源和采集记录，放入 `data/raw/`；
3. 对职位待遇、职位描述、公司介绍、公司规模、公司性质和行业等字段完成映射，统计空值和异常格式；
4. 实现去重、缺失值处理、薪资统一、地区和经验标准化，输出 `data/processed/jobs.csv`；
5. 用正常采集结果、空文件和缺字段文件分别运行清洗脚本，记录处理结果；
6. 把清洗后字段、文件路径和处理规则发给卢世豪、丁伟哲和徐挚凌，等待他们确认接口。
