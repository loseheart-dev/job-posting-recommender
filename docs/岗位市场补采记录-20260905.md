# 岗位市场补采记录（2026-09-05）

本次补采针对财务和销售两个占比较低的类别，使用 BOSS 直聘专用 Chrome、上游固定版本和 24 个城市，结果先落到独立 `data/raw/` 目录，再合并清洗到 `data/processed/jobs.csv`。

| 批次 | 关键词数 | 城市数 | 成功任务 | 列表记录 | 合并后总量 |
|---|---:|---:|---:|---:|---:|
| 财务 | 7 | 24 | 168 | 2520 | 20696 |
| 销售（访问受限前） | 7 | 24 | 30 | 450 | 20872 |

合并去重后的岗位类别占比：技术 7015（33.61%）、财务 2204（10.56%）、销售 2774（13.29%）、行政 3500（16.77%）、制造 3021（14.47%）、其他 2358（11.30%）。

财务批次报告：`data/supplement_records/market_supplement_finance_record.json`；销售批次报告：`data/supplement_records/market_supplement_sales_record.json`。原始岗位 JSON 和清洗后的 CSV 按项目规则由 `.gitignore` 管理，不混入代码提交。

销售批次在第 31 个任务起连续出现上游响应未捕获，随后 BOSS 页面明确提示 IP/账号访问受限。已停止采集，没有绕过限制或用空结果补数；剩余销售任务需平台恢复后再执行。

验证：`.venv/bin/pytest -q`（74 passed）；`tests/verify_real_data.py` 和 `tests/verify_app_integration.py` 均通过。
