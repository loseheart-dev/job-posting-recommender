# D5 日报（25051430 徐挚凌）

## 昨日遗留问题的回答
D4 计划中的 Z7 页面联调和 README 启动说明已完成。

## 今日目标
- 与王宇杰联调五模块页面，确认页面服务调用契约。
- 补充 README、服务调用契约，并将 AppTest 联调纳入 CI。

## 实际进展
- README 和 `docs/25051430/服务层调用说明.md` 提交 PR #30，已合并 main。
- 同步 main（含 PR #29、#33、#34，提交 `8dc8381`），逐页核对 7 个 `analysis_service` 函数及 `site_service`、`task_service`、`job_service` 调用，接口一致。
- AppTest 脚本初始提交 `ae5998e`，PR #36 已合并 main。提交 `3b54c8b` 后，岗位检索、个性化推荐、采集管理执行真实交互，市场概览和分析洞察断言页面输出；检查 `at.exception` 和分析页“不可用” warning，CI 增加显式步骤，并恢复 jobs.csv 时间戳。

## 遇到的问题与解决
只检查未捕获异常不足以证明服务联通，因此增加真实交互和结果断言；`unittest discover` 不会发现联调脚本，因此在 CI 显式执行；测试改写 `data/processed/jobs.csv` 会污染数据更新时间，因此用 `os.utime` 恢复时间戳。

## 明日计划
进入 R7 全链路演示，补充个人成果记录和提示词导出。
