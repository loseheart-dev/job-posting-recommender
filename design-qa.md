# Design QA

## 对比基线

- 设计源：`docs/团队/assets/前端设计稿-v1.png` 及其余四个入口补充稿。
- 实现：`src/ui/pages.py` + `.streamlit/config.toml`。
- 数据：`data/processed/jobs.csv`，8,608 条岗位。
- 视口：1536×1024 设计尺寸、1280×720 常用桌面尺寸、536 px 窄屏。

## 视觉检查

- 布局：五个入口均使用固定左侧导航、顶部数据状态、小圆角卡片和宽屏工作台网格；未发现横向溢出。
- 颜色：蓝/绿/橙状态色与设计 Token 一致，文字和表面对比度清晰。
- 排版：使用系统中文字体回退，标题、指标、说明三级层次可区分，长岗位文本不覆盖相邻区域。
- 图表：Plotly 图表均有标题和单位；上界超过 40,000 元的低频区间合并为 `40000+`，解决离群值导致的坐标轴不可读。
- 窄屏：页头、推荐卡和站点配置在媒体查询下顺序堆叠，未发现重叠或裁切。
- 素材：运行页面不加载设计稿 PNG，不使用假图表、CSS 插画或额外动效库。

## 功能与状态

- 导航：五个入口均可切换，激活态清晰。
- 筛选：“Python + 杭州”将结果从 8,608 条缩小为 12 条，详情同步更新。
- 接口：学历图表使用 `education_distribution`；概览和检索结果使用 `filter_jobs` → `paginate_jobs`，结果总数与页码元数据来自同一返回值。
- 分页：检索页实际切换到第 2 页，表格与详情均随当前页同步。
- 推荐：目标岗位“数据分析师”、期望城市“杭州”、技能“Python;SQL”成功生成 5 条多因素推荐。
- 空值：缺失技能显示“暂无”，不再渲染 `nan`；空数据、无筛选结果、模型错误和采集错误均有独立提示路径。
- 可访问性：表单有可读标签，使用原生键盘可达控件，动效遵循 `prefers-reduced-motion`。
- 控制台：未发现应用 error 或 `use_container_width` 弃用 warning。

## 验证命令

- `.venv/bin/python -m pytest -q`：64 passed。
- `.venv/bin/python -m tests.verify_app_integration`：五模块渲染、搜索、分页第 2 页、推荐提交和采集触发通过。
- `.venv/bin/python -m py_compile src/ui/pages.py`：通过。
- `git diff --check`：通过。

## 接受的差异

- 展示数字、岗位、公司和任务状态使用真实运行结果，不复制设计稿示意值。
- 输入框、表格和单选控件保留 Streamlit 原生行为，外观通过集中 CSS 贴合设计。
- 导航使用原生单选标记，未为装饰性图标引入额外素材或依赖。

final result: passed
