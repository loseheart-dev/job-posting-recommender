# 组员报告：后端数据采集、字段解析与清洗：卢世豪

## 一、先把整个项目讲明白

项目名称是“面向大学生求职的岗位数据分析与个性化推荐系统”。项目从智联招聘、前程无忧（51job）等在线招聘平台采集大数据相关岗位信息，提取职位名称、职位待遇、职位描述、公司介绍、公司规模、公司性质、行业、学历、经验和技能等字段，经过清洗后交给算法、服务和页面使用。

完整链路为：

```text
在线招聘平台 → 卢世豪采集、字段解析与清洗 → 徐挚凌数据服务
→ 郑维豪主题分析与模型 → 丁伟哲个性化推荐 → 王宇杰页面展示
```

系统首期必须完成智联招聘和 51job 的限定范围采集，提供 BFS 和 DFS 两种自研网页遍历策略，保存原始数据和采集记录，再输出统一的清洗数据。系统不做真实投递、聊天、简历存储，也不采集手机号、账号 Cookie 等敏感信息。

## 二、我的职责

我负责后端数据入口，具体包括：

1. 确定智联招聘和 51job 的岗位关键词、城市范围、采集时间和访问规则；
2. 完成两个平台的岗位信息采集，保存原始结果、来源、采集时间和任务记录；
3. 实现 BFS 和 DFS 两种网页遍历策略，记录访问节点、策略和采集结果；
4. 分别完成两个平台的页面字段解析，再映射到统一岗位字段；
5. 提取职位名称、职位待遇、职位描述、公司介绍、公司规模、公司性质、行业、学历、经验和技能等信息；
6. 完成字段统一、去重、空值标记、薪资统一、地区和经验标准化；
7. 保留原始数据，不覆盖原文件，不保存个人联系方式、Cookie 和账号凭据；
8. 输出清洗后的 `data/processed/jobs.csv`、数据字典和采集/清洗记录，供郑维豪、丁伟哲、徐挚凌和王宇杰使用。

我不负责主题分析、模型训练和前端页面。算法或页面发现字段问题时，我先根据原始数据和处理记录定位，再调整清洗规则，不让 AI Agent 自行猜测字段含义。

## 三、必须交付的成果

- 智联招聘和 51job 的平台、采集范围和使用规则说明；
- 两个平台的采集脚本、网站适配器和运行说明；
- BFS/DFS 遍历代码、策略说明和运行记录；
- 原始岗位数据和采集任务记录；
- 字段解析和统一映射规则；
- 可重复运行的数据导入、去重和清洗脚本；
- 清洗后的 `jobs.csv`、数据字典和处理日志；
- 清洗前后记录数、重复数、缺失值和异常薪资统计；
- 一个正常数据案例、一个空数据或缺字段案例；
- 给算法和后端服务成员的字段说明、路径和运行记录。

## 四、验收标准

- 智联招聘和 51job 均有可复现的限定范围采集结果，能够说明平台、关键词或城市范围和采集时间；
- BFS 和 DFS 均能运行，运行记录包含策略、访问节点和结果数量；
- 核心岗位字段有明确类型、单位和来源，缺失情况有记录；
- 相同输入重复运行，清洗结果基本一致；
- 重复岗位、缺失薪资、空描述和异常薪资都有明确处理或标记；
- 原始数据不被覆盖，采集过程不绕过登录、验证码或访问限制；
- 空文件、缺字段和坏格式能够给出清楚提示；
- 郑维豪和徐挚凌可以直接读取清洗结果，不需要手工复制粘贴；
- 不提交个人信息、Cookie、密钥或未经确认的大型原始文件；
- 我本人能说明采集范围、遍历策略、字段映射、清洗规则和数据限制。

## 五、交给 AI Agent 的任务单

```text
目标：完成智联招聘和 51job 岗位数据的限定范围采集、字段解析、导入和清洗。
先做：阅读 AGENTS.md、五人任务分工与验收清单、接口约定和现有代码，先确认实际字段。
输入：选定平台的岗位页面或公开岗位信息，不得假设不存在的列。
输出：网站适配器、BFS/DFS 遍历、原始数据、字段映射、清洗脚本、jobs.csv、数据字典、处理日志和验证命令。
限制：不得绕过登录、验证码或访问限制；不得采集个人联系方式；不得覆盖原始数据；不得上传数据到外部服务；缺失值不得静默删除；不得改动算法、服务和前端文件；不得新增无必要依赖。
验收：两个平台均有结果；BFS 和 DFS 均有运行记录；正常数据可清洗；空文件、缺字段和坏格式有提示；重复运行结果一致；最后列出修改文件和剩余问题。
```

AI Agent 修改后先查看 `git diff`，再用正常文件、空文件和缺字段文件运行验证，最后提交到 `feat/lushihao-data-cleaning` 分支。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责在线招聘平台的数据采集、字段解析和清洗。项目从智联招聘、51job 等平台采集大数据相关岗位，我先限定关键词、城市和采集时间，再用 BFS 和 DFS 两种策略组织网页访问。采集后分别解析职位、薪资、描述、企业和要求字段，统一成岗位表，处理重复、缺失、薪资、地区和经验格式，同时保留原始数据及处理记录。这样算法成员可以直接使用同一份数据，后端服务和前端页面也能追溯数据来源。我不会绕过平台限制，也不会用虚假岗位记录补齐数据。”

### 可能被问的问题

**问：为什么需要实现 BFS 和 DFS 两种策略？**

答：BFS 适合按层访问列表页和详情页，DFS 适合沿链接深入访问。实现两种策略既满足任务要求，也便于比较不同遍历方式。首期限定关键词和城市范围，保证结果可以重复验证。

**问：缺失薪资怎么处理？**

答：保留缺失标记，不把缺失薪资填成 0。统计时说明有效样本范围，模型只使用满足条件的记录。

**问：怎么证明清洗有效？**

答：展示清洗前后记录数、重复数、缺失值、异常薪资和字段类型，并用同一份输入重复运行，比较输出是否一致。

## 七、协作接口

- 给郑维豪：清洗后的岗位表、数据字典、字段来源、缺失值规则和样例数据；
- 给丁伟哲：学历、经验、技能、薪资、地区、公司性质、规模和行业等字段说明；
- 给徐挚凌：原始数据路径、清洗数据路径、任务记录字段和错误状态；
- 给王宇杰：清洗前后统计、字段标签、单位和数据来源说明；
- 字段需要变更时，先在群里说明原因，记录影响范围，确认后再改文档和代码。

## 八、不要承诺的内容

不要承诺一次抓取所有招聘网站、持续实时更新全部岗位或绕过登录、验证码和访问限制。首期只承诺完成智联招聘和 51job 的限定范围采集，数据来源和处理过程可追溯，清洗结果可以被算法、服务和页面重复使用。

## 九、下一步小步骤

1. 确定两个平台的岗位关键词、城市范围、采集时间和使用规则；
2. 完成两个网站的最小采集适配，先把原始结果放入 `data/raw/`；
3. 跑通 BFS 和 DFS，保存访问节点、策略和采集结果记录；
4. 完成职位名称、职位待遇、职位描述、公司介绍、公司规模、公司性质、行业、学历、经验和技能的字段映射；
5. 实现去重、空值标记、薪资统一、地区和经验标准化，输出 `data/processed/jobs.csv`；
6. 编写数据字典和清洗记录，用正常数据、空文件、缺字段和坏格式分别验证；
7. 将字段、路径、规则和验证结果交给郑维豪、丁伟哲和徐挚凌，等待服务和算法联调。
