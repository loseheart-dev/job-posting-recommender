# D5 日报

## 昨日遗留问题的回答
①"D2 基础"可回验：`sample_jobs.csv`、`sample_data.py` 与 `schema.py` 的 `SALARY_FACTOR_COLUMNS / SKILL_GRAPH_KEYS / COMPANY_PROFILE_COLUMNS` 首次入库均为提交 **`99e8fbe`**（"feat: 完成三类主题分析与聚类、薪资预测算法模块"），对应 ZW1/ZW2。
② 三种折算方案实验脚本（无条件×174／加高值上限／只保留明确月薪）为本地一次性脚本，未入库；R² 影响（0.1754→-0.6789→0.1943）与"只保留明确月薪"结论已固化于 PR #32（`6dc1dfe`）及 #33/#35 评审，以合并代码为准。

## 今日目标
1. 同步 main；2. 用真实数据（expanded_jobs.csv 8608 行）完成 L6 集中联调；3. 更新三份个人交付物并归档。

## 实际进展
- L6 集中联调通过：expanded_jobs.csv 置入本地 jobs.csv（gitignore），load_jobs→job_service（avg=11568、北京 299 条、52 桶）→算法五函数（53 因子/133 节点/6349 企业/3 群组/R²=0.1754）→推荐 Top5→页面 Z7 五模块 PASSED；
- 复测文档提交 `01386f3`，PR #38 合并（`1ffc968`）；
- 分支同步 main 至 `f883c16`。

## 遇到的问题与解决
- 页面脚本缺 streamlit/plotly → pip 安装后 Z7 通过；
- recommend_jobs 参数顺序 (profile, jobs) 初用反序 → 查签名修正；
- 正式 jobs.csv 未交付 → 用 fixtures 等效联调；
- 折算脚本未入库 → 结论以 PR 合并代码为准。

## 明日计划
答辩演示收尾：真实数据页面跑通一次，同步组长确认 L6/R7 勾选与课程提交目标。
