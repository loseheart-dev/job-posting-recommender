# 组员报告：算法统筹——卢世豪

## 一、先把整个项目讲明白

项目名称是“面向大学生求职的岗位数据分析与个性化推荐系统”。它不是要复制 BOSS 直聘，而是利用公开岗位数据，帮助大学生回答三个问题：岗位市场需要什么技能、不同岗位的薪资和地区情况如何、某个学生适合哪些岗位以及还缺什么能力。

完整链路为：

```text
公开岗位数据 → 郑维豪清洗 → 徐挚凌封装数据服务
→ 算法处理 → 王宇杰页面展示 → 用户筛选或填写画像后得到结果
```

三类算法是：KMeans 岗位聚类、随机森林薪资预测、TF-IDF 加余弦相似度岗位推荐。前端用 Streamlit，数据处理用 pandas，算法用 scikit-learn。系统只做分析和推荐，不做真实投递、聊天、简历存储，也不采集手机号、账号 Cookie 等敏感信息。

## 二、我的职责

我负责算法统筹，具体完成两项算法：

1. **KMeans 聚类**：根据岗位类别、技能、地区、经验和薪资等特征，把岗位划分为若干岗位群组，并解释每一组的典型特征；
2. **随机森林回归**：根据岗位类别、地区、经验、教育、技能数量等特征预测薪资，输出 MAE、R² 等指标，并说明模型局限。

我不负责修改原始数据清洗规则，也不负责独自完成前端页面。发现字段问题时反馈郑维豪，需要页面接入时向徐挚凌提供稳定的函数输入输出。

## 三、必须交付的成果

- 算法训练/预测代码或 Notebook；
- 特征说明：使用哪些字段、为什么使用、如何编码；
- KMeans 的聚类结果图和岗位群组解释；
- 随机森林的预测结果、测试指标和至少一个对比结果；
- 一个正常案例和一个异常/空数据案例；
- 给徐挚凌调用的函数说明；
- 一分钟个人讲解稿、运行截图和 AI Agent 关键提示词。

## 四、验收标准

- 从郑维豪提供的清洗数据可以重新训练，不依赖手工改数据；
- KMeans 能输出每条岗位的 cluster 标签，并能说明每个群组的主要特征；
- 薪资预测不能只展示训练集结果，必须有测试集或明确的验证划分；
- 至少报告 MAE 或 R²，并能解释指标含义；
- 输入字段缺失、数据量不足时返回提示，不直接崩溃；
- 徐挚凌可以通过约定函数拿到预测结果，王宇杰可以在页面展示；
- 我本人能现场解释特征、训练流程、指标和局限。

## 五、交给 AI Agent 的任务单

```text
目标：在不改变现有目录和技术栈的前提下，完成岗位 KMeans 聚类和随机森林薪资预测。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、现有数据字段和已有代码。
输入：清洗后的岗位数据，字段以实际数据为准，不得自行虚构字段。
输出：算法代码、训练结果、评价指标、可复现运行步骤和一个异常案例。
限制：不得覆盖原始数据；不得新增无必要依赖；不得修改前端和他人负责的清洗逻辑；不能把训练集指标冒充测试集指标。
验收：能重新训练；聚类有群组解释；回归有 MAE 或 R²；空数据和缺字段有明确提示；最后列出修改文件和验证命令。
```

AI 生成代码后，我必须自己运行、看 `git diff`、解释核心代码，再提交到 `feat/lushihao-algorithm` 分支。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责项目的算法统筹，主要完成岗位聚类和薪资预测。聚类用于发现岗位市场中具有相似技能和经验要求的岗位群组，帮助学生看到不同职业方向；薪资预测用于分析岗位特征与薪资之间的关系。我的算法输入来自后端清洗后的数据，输出包括岗位群组、预测值和评价指标，再由联调模块提供给前端。我们不会只展示一个模型分数，而是同时说明特征、测试方法和模型局限。”

### 可能被问的问题

**问：为什么选 KMeans？**

答：岗位群组没有现成标签，KMeans 是适合无监督分组的基础方法，结构清楚、实现成本低，适合课堂项目。我们会通过特征标准化和轮廓系数辅助选择聚类数量。

**问：随机森林预测薪资是否可信？**

答：它只能反映数据样本中的统计关系，不等于真实薪资承诺。我们会使用独立测试数据评价，并在页面注明薪资缺失、地区差异和数据来源限制。

**问：AI 写的代码你真的理解吗？**

答：我负责输入输出、特征处理、训练过程和指标验证，代码生成后由我运行并检查正常和异常案例，保留了提示词、修改理由和验证记录。

## 七、协作接口

- 向郑维豪索取：清洗后字段名、缺失值规则、样例数据；
- 提供给徐挚凌：训练函数、预测函数、输入字段和返回字段；
- 提供给王宇杰：聚类分布图、薪资预测结果和指标字段；
- 与丁伟哲统一：岗位文本字段、技能标签格式和测试画像。

## 八、不要承诺的内容

不要承诺实时抓取所有招聘网站、准确预测个人真实薪资、自动投递简历或使用大模型替代招聘决策。明天的核心承诺是：公开数据可复现、三类算法能运行、结果可以解释。

## 九、下一步小步骤

1. 今天先向郑维豪确认清洗后数据的字段名、技能格式、薪资单位和缺失值规则；
2. 用一小份真实数据跑通 KMeans，确定聚类特征和群组数量，并输出群组统计；
3. 完成随机森林薪资预测，固定训练集和测试集划分，记录 MAE 或 R²；
4. 把两个算法的函数签名、输入字段、返回字段和异常状态交给徐挚凌；
5. 用正常数据和缺字段数据验证，再把一组可展示结果交给王宇杰。
