# 组员报告：前端开发与可视化：王宇杰

## 一、整个项目是什么

项目首期采集 BOSS 直聘岗位数据，面向大学生实习和校招提供岗位检索、岗位市场分析、算法结果展示和个性化推荐。页面不是静态 PPT：用户改变筛选条件或学生画像后，必须调用后端和算法模块得到真实结果。其他平台接入后复用同一页面，不为待定平台预先制作假数据。

完整链路为“卢世豪采集、解析并清洗在线岗位数据 → 徐挚凌管理采集任务并提供服务函数 → 郑维豪完成三类主题分析、丁伟哲完成多因素推荐 → 我负责页面输入、图表和结果展示”。技术栈是 Python、Streamlit、pandas 和 scikit-learn。

## 二、我的职责

- 使用 Streamlit 完成一个可运行的 Web 页面；
- 完成网站列表、BOSS 采集任务、岗位检索、数据仪表盘、算法结果和个性化推荐功能区；
- 对接徐挚凌的数据读取、筛选、统计和模型调用函数；
- 展示岗位数量、技能热度、薪资分布、薪资影响因素、能力需求图谱、企业画像和推荐结果；
- 接收学历、专业、学校、求职地、工作年限、技能、工作经历和岗位名称，展示待遇水平、个人待遇区间、适配公司和匹配概率；
- 处理空数据、无结果、模型未加载和输入错误状态。

我不负责在页面中重新实现算法，不在代码里写死图表数据，不为了视觉效果隐藏错误。

## 三、我的交付物

1. Streamlit 页面代码；
2. 网站和采集任务管理、岗位检索、数据分析、个性化推荐的输入控件和结果展示；
3. 至少三种有意义的可视化图表；
4. 一条从筛选到推荐的完整演示路径；
5. 正常数据和空/异常数据截图；
6. 页面启动说明；
7. 一分钟讲解稿、AI Agent 关键提示词和验证记录。

## 四、验收标准

- 一条命令可以启动页面；
- 至少有网站采集管理、岗位检索、数据仪表盘、算法结果、个性化推荐功能区；
- 筛选条件变化会改变表格、统计或图表；
- 求职者画像变化会改变待遇分析、适配公司或推荐排序；
- 薪资因素、能力需求图谱和企业画像都有对应展示；
- 图表有标题、图例、单位或数据范围说明；
- 推荐结果显示岗位、分数、匹配技能、缺失技能和推荐理由；
- 数据为空、筛选无结果、模型未加载时页面给出可理解提示；
- 页面不直接读原始数据，不硬编码最终结果；
- 我本人能解释每个控件如何调用后端、每张图表达什么。

## 五、交给 AI Agent 的任务单

```text
目标：使用现有 Python 技术栈完成采集管理、岗位分析与多因素推荐 Streamlit 页面。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、徐挚凌提供的服务函数和真实返回字段。
输入：网站配置和采集任务状态、岗位筛选条件、三类主题分析结果和多因素推荐结果。
输出：采集管理区、岗位检索区、数据分析区、个性化推荐区、至少三种图表、异常提示、启动说明和演示截图。
限制：不得在页面硬编码岗位数据或模型结果；不得重新实现算法；不得新增无必要前端框架；不得隐藏采集和服务错误。
验收：一条命令启动；网站和采集任务状态可展示；筛选和画像输入会影响结果；三类主题分析和推荐结果可展示；采集失败、空数据和模型缺失有提示；图表标题和图例完整；列出修改文件和验证步骤。
```

AI Agent 修改后先看 diff，再逐项点击页面控件；不要只确认页面“能打开”，必须确认输入确实改变了输出。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责前端和可视化。页面包含网站采集管理、岗位检索、数据分析和个性化推荐区域。管理员可以查看网站配置和采集任务状态，用户可以按条件筛选岗位，再填写学历、专业、学校、技能和工作经历获得待遇分析和岗位推荐。页面中的数据和图表都由后端服务返回，能够展示工资影响因素、能力需求图谱、企业画像和匹配概率。对于采集失败、无数据和模型未加载等情况，页面会给出提示。”

### 可能被问的问题

**问：页面如何证明不是静态图？**

答：现场修改采集范围、地区、岗位关键词或学生画像，任务状态、表格、图表或推荐排序会跟着变化；页面不保存一套写死的最终结果。

**问：为什么选 Streamlit？**

答：课程目标是快速完成 Python 数据分析 Web 原型，Streamlit 能直接承载控件、表格和图表，减少前端工程量，把时间用于数据和算法解释。

**问：页面最重要的可视化是什么？**

答：技能热度帮助学生了解市场需求，薪资分布帮助形成预期，地区/经验对比帮助筛选方向；每张图都对应一个求职问题，而不是为了凑图表数量。

## 七、协作接口

- 服务函数、返回字段、数据字典和算法样例以《接口约定》为准；
- 使用固定返回样例独立完成页面控件、图表和异常状态；
- 页面只依赖服务接口，不直接依赖采集和算法实现；
- 发现后端返回不一致时先记录错误，不在页面里偷偷兼容多套格式；
- 完成后给组长一条可重复演示路径。

## 八、不要做的事

不要先做复杂登录、权限、响应式大屏或动画。明天答辩最重要的是采集管理、数据分析和个性化推荐主链路能跑通、输入能影响结果、图表说得清楚；这些完成后再考虑美化。

## 九、下一步小步骤

1. 在 `app.py` 和 `src/ui/` 中搭好网站管理、采集任务、岗位检索、分析和推荐页面；
2. 按《接口约定》制作网站配置、任务状态和岗位筛选返回样例；
3. 用固定样例完成工资因素、能力需求图谱、企业画像和推荐结果展示，给每张图补充标题、图例和单位；
4. 制作求职者画像和推荐结果卡片，显示待遇区间、企业画像、匹配概率、匹配技能、缺失技能和推荐理由；
5. 用正常数据、采集失败、无筛选结果、文件缺失和模型未加载五种状态完成个人验收；阶段审核通过后再接真实服务。
