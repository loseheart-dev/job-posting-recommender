# 五人任务分工与验收清单

## 项目信息

- 项目名称：`面向大学生求职的岗位数据分析与个性化推荐系统`
- 组长：`[待确认]`
- 成员：`卢世豪`、`丁伟哲`、`郑维豪`、`徐挚凌`、`王宇杰`
- 指定验收人：组长 + 对应任务的另一名成员

姓名和组长确定后，只替换对应占位内容，不改变验收边界。

## 总体交付目标

项目最终必须能够完成一条完整链路：

```text
在线招聘平台数据采集 → 原始数据保存 → 数据清洗 → 算法分析 → 结果可视化 → 用户操作得到结果
```

每名成员都要有一个可以独立演示 1—2 分钟的成果。只负责“协助”“查资料”或“调用 AI”不算独立交付物。

## 必须实现的项目要求

以下内容属于基础版本必须实现的功能，创新功能在基础版本完成后再安排：

1. 采集智联招聘、前程无忧（51job）等招聘平台的大数据相关岗位信息，提取职位名称、职位待遇、职位描述、公司介绍、公司规模、公司性质、行业、学历、经验和技能等字段；
2. 支持网站列表增删改查、启停、采集开始时间和采集频率配置，并预留采集器水平扩展能力；
3. 支持深度优先和广度优先爬取策略，提供项目自研的遍历算法；
4. 完成薪资影响因素分析、岗位能力需求图谱和招聘企业画像；
5. 根据求职者学历、专业、学校、求职地、工作年限、技能、工作经历和岗位名称，输出待遇水平、个人待遇区间、适配公司、公司性质与规模、行业和匹配概率；
6. 完成职位数据获取与预处理、职位数据可视化分析、多元职位推荐模型、职位信息展示和个性化推荐系统。

大数据处理使用 PySpark/Spark 或其他开源大数据组件，pandas 作为清洗和小样本处理工具；采集、分析和展示必须形成可运行的平台链路。

基础功能完成后，才安排多平台实时监控、增量采集、趋势预测、复杂模型和基于反馈的推荐优化等创新功能。

## 已冻结的共享接口

为减少互相等待，岗位表、学生画像、筛选参数和算法返回字段统一写在《接口约定.md》中，代码实现位于 `src/data/schema.py`。

- 郑维豪负责在线岗位数据采集、原始数据说明和按约定输出 `data/processed/jobs.csv`，不自行改列名；
- 卢世豪负责审核字段、单位和算法输出变更；
- 徐挚凌负责把采集管理、固定字段和算法调用封装成服务函数；
- 丁伟哲和王宇杰分别使用固定的推荐输入和服务返回，不直接读取原始数据；
- 任何字段变更先在群里说明原因，待卢世豪确认后再改文档和代码。

联调分五个节点：采集基线、清洗基线、分析与推荐基线、服务基线、页面联调。每个人先在个人分支完成正常和异常案例，卢世豪审核通过后再合并到 `main`。

## 任务分工

每个人只修改自己的任务目录。需要改共享接口时，先在群里确认字段和函数签名，再提交代码。

### 卢世豪：算法统筹，负责三类主题分析和模型评价

主要任务：把清洗后的岗位数据转换成可用于建模的特征，完成岗位工资影响因素分析、岗位能力需求图谱、招聘企业画像、KMeans 聚类和随机森林薪资预测，并向后端提供可调用函数。

下一步小步骤：

1. 从郑维豪处拿到清洗后数据样例，确认岗位、学历、专业要求、地区、经验、技能、公司和薪资字段；
2. 完成岗位工资影响因素分析，说明学历、经验、地区、技能、公司性质和公司规模等因素与薪资的关系；
3. 完成岗位能力需求图谱，统计技能节点及其共现关系，并完成招聘企业画像；
4. 在 `src/algorithms/clustering.py` 中完成特征选择、标准化、KMeans 训练，输出每条岗位的群组标签和每个群组的特征统计；
5. 在 `src/algorithms/salary.py` 中完成训练集/测试集划分、随机森林回归和 MAE 或 R² 计算；
6. 用正常数据、缺字段数据各运行一次，把函数输入、输出和异常提示交给徐挚凌；
7. 给王宇杰提供三个主题分析、聚类图、薪资预测结果和指标字段，完成一次页面联调。

交付物：分析与模型代码、特征说明、薪资因素结果、能力需求图谱、企业画像、聚类与预测样例、指标记录和调用说明。

验收标准：三个主题均有真实岗位数据输入和可解释输出；聚类结果包含群组解释；薪资预测使用独立测试数据；有 MAE 或 R²；数据量不足或字段缺失时有明确提示；徐挚凌能调用，王宇杰能展示。

### 丁伟哲：算法成员，负责求职者画像和多元职位推荐

主要任务：根据学历、专业、学校、求职地、工作年限、技能、工作经历和岗位名称构建求职者画像，结合岗位与企业特征完成多元职位推荐，输出待遇水平、个人待遇区间、适配公司、公司性质与规模、行业、匹配概率和推荐理由。

下一步小步骤：

1. 和郑维豪确认岗位的学历、经验、技能、薪资、公司性质、规模和行业字段；
2. 和徐挚凌确认求职者画像字段：学历、专业、学校、目标岗位、技能、求职地、工作年限和工作经历；
3. 在 `src/algorithms/recommender.py` 中完成岗位文本 TF-IDF 与余弦相似度计算，并加入学历、地区、经验、技能和薪资等因素；
4. 输出待遇水平、个人待遇区间、适配公司、公司性质与规模、行业、匹配概率、匹配技能、缺失技能和推荐理由；
5. 准备技术岗、数据岗、技能为空三组画像，测试空岗位、缺失字段和筛选无结果情况，把稳定返回格式交给徐挚凌和王宇杰。

交付物：`recommender.py`、求职者画像字段说明、多因素计算说明、3 组测试画像、待遇分析、Top-K 结果和推荐解释。

验收标准：学历、专业、学校、地区、工作年限、技能、工作经历或目标岗位变化时，排序、待遇区间或解释会变化；结果不是写死的；每条推荐包含岗位、待遇区间、公司画像、匹配概率、匹配技能和缺失技能；无结果时有提示；徐挚凌能调用，王宇杰能展示。

### 郑维豪：后端成员，负责在线岗位数据采集、清洗和数据管理

主要任务：从选定的在线招聘平台采集岗位信息，把原始数据整理成算法和页面都能使用的统一 CSV，保留来源、采集范围、原始数据说明和处理记录。

下一步小步骤：

1. 确认首期平台、岗位关键词、城市和采集范围，记录平台规则和数据来源；
2. 实现或整理岗位信息采集流程，将原始结果放在 `data/raw/`，不覆盖、不修改已有原文件；
3. 统计原始数据的字段、记录数、重复值、空值和薪资格式，补充数据字典；
4. 在 `src/data/cleaner.py` 中完成去重、缺失值处理、薪资统一、地区和经验字段标准化；
5. 输出 `data/processed/jobs.csv`，同时记录采集和清洗前后记录数、异常处理方式及来源；
6. 用正常采集结果、空文件、缺字段文件各运行一次，把字段名、路径和缺失规则交给卢世豪、丁伟哲、徐挚凌。

交付物：采集说明或采集脚本、原始数据说明、`cleaner.py`、清洗后 `jobs.csv`、数据字典和采集/清洗处理记录。

验收标准：原始文件不被覆盖，采集结果能说明平台、范围和时间；采集过程不绕过登录、验证码或访问限制，不保存个人联系方式、Cookie 和账号凭据；采集结果来自在线招聘平台，不能用手工编造的岗位记录替代；脚本可以重复运行；清洗前后记录数和缺失值可说明；空文件、缺字段和坏格式有提示；算法和后端使用同一份字段约定。

### 徐挚凌：后端成员，负责采集管理、调度、数据服务和前后端联调

主要任务：把网站列表管理、采集任务调度、数据读取、岗位筛选、统计和算法调用封装成页面可以直接使用的服务函数，支持采集器按网站水平扩展并跑通完整链路。

下一步小步骤：

1. 根据郑维豪的采集配置和数据字典，固定原始数据、清洗数据路径及缺文件提示；
2. 实现网站列表的新增、查询、修改、删除、启停和采集频率配置；
3. 实现采集任务触发、开始时间、运行状态和记录查询，支持不同网站采集器接入同一调度流程；
4. 在 `src/services/job_service.py` 中完成岗位关键词、地区、学历、经验、工作方式、行业和薪资筛选；
5. 增加岗位数量、技能频率、薪资分布等统计函数，统一返回字段和空结果状态；
6. 接入卢世豪、丁伟哲的分析和推荐函数，保证页面不直接读取原始数据，也不重复写模型逻辑；
7. 依据王宇杰的页面调用需求完成联调，补充 README 启动步骤和一条可重复演示路径。

交付物：采集管理与调度服务、数据读取模块、服务模块、调用示例、任务运行记录、联调记录和 README 启动说明。

验收标准：管理员可以管理网站列表、启停采集任务并设置开始时间和频率；新增网站可以按统一接口接入；前端通过服务函数拿到数据；筛选条件会影响统计和算法输入；模型未加载、文件缺失、采集失败和筛选无结果时有明确状态；项目可从采集到页面结果走通一条演示链路。

### 王宇杰：前端成员，负责采集管理、分析展示和个性化推荐页面

主要任务：把网站列表和采集任务管理、岗位检索、市场统计、三类分析、求职者画像和个性化推荐做成可操作页面，确保输入变化能反映到结果。

下一步小步骤：

1. 在 `app.py` 和 `src/ui/` 中搭好页面布局，先放置网站管理、采集任务、岗位检索、数据分析和个性化推荐区域；
2. 接入徐挚凌的网站配置和采集任务服务，完成平台启停、开始时间和频率展示；
3. 接入岗位筛选、统计、薪资因素、能力需求图谱和企业画像，完成数据可视化；
4. 完成学历、专业、学校、求职地、工作年限、技能、工作经历和岗位名称输入，展示待遇水平、个人待遇区间、适配公司和匹配概率；
5. 用正常数据、采集失败、筛选无结果、文件缺失和模型未加载状态测试页面，并保留截图和演示路径。

交付物：`app.py`、`src/ui/` 页面代码、网站和采集任务管理界面、分析图表、求职者画像、推荐展示和演示截图。

验收标准：一条命令可以启动；管理员可以看到网站和采集任务状态；画像输入变化会改变待遇分析、适配公司或推荐排序；薪资因素、能力需求图谱和企业画像有对应展示；图表有标题和图例；页面不写死结果；采集失败、空数据和模型未加载时有提示。

> 组长从五人中确认一人，负责 README、合并、进度和最终演示，但不能替代其他成员的独立交付。

## 每个人的提交清单

提交前逐项确认：

- [ ] 我完成了任务卡中的代码或文档；
- [ ] 我在本地运行过至少一个正常案例；
- [ ] 我验证过一个异常或空数据案例；
- [ ] 我看过 `git diff`，没有无关改动；
- [ ] 我写清楚了运行命令和验证结果；
- [ ] 我能在答辩中解释自己的核心代码；
- [ ] 我使用 AI 生成的部分已经实际检查，不是直接照搬。

## 合并前清单

组长合并前确认：

- [ ] 五人的分支都有有效提交；
- [ ] 平台、采集范围、数据来源、字段和算法名称已补齐；
- [ ] 项目可以从头启动并走通一条演示链路；
- [ ] 三类主题分析和推荐模型都有实际输入、输出和评价/解释；
- [ ] 页面有异常提示，不能运行的功能已明确标注；
- [ ] README、演示截图和任务完成状态一致；
- [ ] 没有密钥、Cookie、个人信息和无关大文件。

## 简单进度节奏

| 时间 | 全组目标 |
|---|---|
| 第一天 | 定题、定采集平台和范围、定三类算法，完成页面草图和分工确认 |
| 第二天 | 数据采集和清洗可运行，页面骨架可启动，算法输入输出定稿 |
| 第三天 | KMeans、随机森林、TF-IDF 推荐分别跑通一个样例，完成基础图表 |
| 第四天 | 模块集成，补充评价指标和异常处理，准备中期检查 |
| 第五天 | 完成联调、演示流程、README 和个人成果记录 |
| 第六天 | 全流程验收、录制演示、修复阻塞问题、整理报告 |

## 阻塞记录

| 日期 | 成员 | 问题 | 已尝试 | 需要的帮助 | 状态 |
|---|---|---|---|---|---|
| `[日期]` | `[姓名]` | `[具体问题]` | `[已尝试内容]` | `[需要谁协助]` | `[待处理/已解决]` |
