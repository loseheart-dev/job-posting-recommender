# D5 日报

## 昨日遗留问题的回答

终态以 `815f12d`、`d50bbc5` 为准：`8fc03e5` 清洗侧按 `salary_text` 只解析明确月薪，非月薪、面议、未知单位的 `salary_min/max/avg` 置空；算法只读 `salary_avg`，`_common.py` 仅标记 `<500` 元/月，统计、RF 保留 ≥10 万月薪。断言覆盖非月薪为空、`100-200K·15薪` 可解析、12.5 标记而 861300/100000/255000 不因金额标记、分桶数等于有效数。

8608 条快照在 `tests/fixtures/real_data_salary_verification.json`（`b8ac5cb`）：有效 7834、≥10 万 19、缺失 774，均值 12337.92；0–5k/5–10k/10–15k/15–20k/20–30k/30–50k/50–100k/≥100k 为 519/3232/2398/669/650/262/85/19。RF `MAE=4970.88`、`R²=0.1937`；D4 的 0.234 是旧方案，差异 -0.0403。

## 今日目标

跟踪补采，核对聚类、图谱和推荐。

## 实际进展

确认技术岗位偏多，补采缺失领域并扩城；跟踪 `e2ccf27` 至 `f883c16`。财务过滤修复已进主线，待复测；完成复算。

## 遇到的问题与解决

补采后技术技能仍偏多，聚类和图谱受全局频数影响，已扩大非技术岗位并看类别内统计。算法脚本曾报 `No module named src`，改用 `PYTHONPATH=.` 后 66 项、BOSS 7 项通过；无关会话排除。

## 明日计划

复核类别占比、技能覆盖率和财务推荐，按哈希查数字。
