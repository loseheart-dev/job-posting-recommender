# 组员报告：后端数据服务与联调——徐挚凌

## 一、整个项目是什么

项目通过在线招聘平台采集岗位数据，为大学生提供岗位检索、市场分析、薪资预测和个性化推荐。郑维豪负责岗位采集和清洗；卢世豪和丁伟哲负责三类算法；王宇杰负责页面。我负责把这些部分接起来，让前端通过稳定的函数获得数据和算法结果，而不是各自读取文件、重复写逻辑。

## 二、我的职责

- 封装统一的数据读取、筛选和统计函数；
- 连接清洗数据、KMeans、随机森林和 TF-IDF 推荐模块；
- 约定函数输入、返回字段和错误提示；
- 处理模型未加载、数据为空、筛选无结果等情况；
- 与王宇杰完成页面联调；
- 负责最终启动说明和从数据到页面的完整演示。

我不负责重写算法核心，不负责替换 Streamlit 或新增大型后端框架。当前项目采用轻量函数调用即可，不为不存在的并发和权限需求提前搭建复杂 API。

## 三、我的交付物

1. 数据读取函数；
2. 岗位筛选和基础统计函数；
3. 三类模型的调用适配；
4. 统一的返回数据结构和错误提示；
5. 联调记录：输入、输出、页面表现；
6. README 中的安装、启动和演示步骤；
7. 从筛选到推荐结果的完整案例。

## 四、验收标准

- 前端不直接读取原始数据，统一调用数据服务；
- 岗位关键词、地区、经验、工作方式和薪资筛选能实际改变结果；
- 统计、聚类、薪资预测、推荐都能从统一入口调用；
- 数据文件不存在、模型文件缺失、筛选无结果时页面不崩溃；
- 新成员按 README 能启动项目；
- 至少有一个完整正常案例和一个空数据/异常案例；
- 只合并已经运行验证的分支；
- 我本人能解释调用链、错误处理和模块边界。

## 五、交给 AI Agent 的任务单

```text
目标：在不改变现有技术栈的前提下，封装岗位数据读取、筛选、统计和模型调用，并完成前后端联调。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、清洗数据字段、算法函数和前端现状。
输入：郑维豪的清洗数据、两位算法成员提供的函数、王宇杰的页面调用需求。
输出：服务函数、调用说明、错误处理、README 启动步骤和联调记录。
限制：不要新增无必要框架；不要复制算法核心；不要在页面硬编码结果；不要改变他人的数据清洗规则。
验收：正常链路能运行；空文件、模型缺失、筛选无结果有提示；列出修改文件、运行命令和剩余问题。
```

AI Agent 修改后先检查 diff，再逐个调用数据读取、筛选、统计和推荐函数，最后才接页面。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责后端数据服务与联调。我的工作不是重新实现三个算法，而是把清洗数据和算法结果封装成前端可以稳定调用的函数。用户输入筛选条件后，服务层读取标准化数据并返回结果；用户填写画像后，再调用推荐模块。系统对文件缺失、模型未加载和无筛选结果都有提示，因此演示不会依赖临时改代码。”

### 可能被问的问题

**问：为什么不直接在 Streamlit 页面里写所有逻辑？**

答：页面只负责输入和展示，数据读取、筛选和模型调用集中在服务函数中，便于测试、复用和说明个人分工，也避免前端重复读原始数据。

**问：如果模型没有训练好，页面怎么办？**

答：页面提示模型未加载；推荐功能可以使用已保存模型或基础规则排序作为降级方案，并明确标注，不伪造模型结果。

**问：你的后端亮点是什么？**

答：亮点是把数据、算法和页面连接成可运行链路，并处理空数据、缺文件和无结果等异常，而不是只让单个 Notebook 跑通。

## 七、协作接口

- 向郑维豪确认清洗结果的路径和字段；
- 向卢世豪、丁伟哲确认模型函数签名和返回字段；
- 向王宇杰提供页面调用示例和错误状态；
- 在合并前要求每个分支提供运行命令和验证证据；
- 负责提醒组长：没有通过验收的模块不能标记完成。

## 八、不要做的事

不要为了“像后端”而额外搭建 FastAPI、数据库服务或权限系统。当前课程项目先用清晰的 Python 模块完成联调；只有现有函数确实无法支持演示时，再讨论升级。

## 九、下一步小步骤

1. 今天先根据郑维豪的数据字典固定 `jobs.csv` 路径和读取方式；
2. 完成关键词、地区、经验、工作方式和薪资筛选函数，统一空结果返回方式；
3. 完成岗位数量、技能频率和薪资分布统计函数；
4. 接入卢世豪和丁伟哲的算法函数，整理模型未加载、缺字段和无结果状态；
5. 给王宇杰提供调用示例，和他一起跑通从筛选到算法结果展示的一条完整路径。
