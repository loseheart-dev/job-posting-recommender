# 组员报告：算法——丁伟哲

## 一、整个项目是什么

本项目面向大学生实习和校招场景，对公开岗位数据进行导入、清洗、分析和推荐。用户可以先查看岗位市场统计，也可以填写目标岗位、掌握技能、期望地区、经验和薪资偏好，系统返回较匹配的岗位并说明理由。

系统链路是“数据导入/采集 → 郑维豪清洗 → 徐挚凌数据服务 → 算法计算 → 王宇杰页面展示”。算法部分共有三类方法：KMeans 聚类、随机森林薪资预测、TF-IDF 加余弦相似度推荐。我负责第三类算法，即个性化推荐。

## 二、我的职责

我负责：

- 设计学生画像字段：目标岗位、技能、地区、经验、期望薪资等；
- 把岗位标题、技能和描述合并成推荐文本；
- 使用 TF-IDF 把学生画像和岗位文本转换为向量；
- 使用余弦相似度计算匹配程度并输出 Top-K；
- 输出匹配技能、缺失技能和推荐理由，不只显示一个分数；
- 准备至少三个不同学生画像，验证推荐结果确实会变化。

不负责修改原始数据，不负责独立完成 Streamlit 页面；需要页面字段时与王宇杰确认，需要数据字段时向郑维豪确认。

## 三、我的交付物

1. 推荐算法代码；
2. 学生画像和岗位文本字段说明；
3. 三个固定测试画像及 Top-K 输出；
4. 匹配技能、缺失技能和推荐解释逻辑；
5. 无岗位、空技能、缺失文本等异常案例；
6. 推荐函数的调用说明；
7. 一分钟讲解稿、截图、运行记录和 AI Agent 关键提示词。

## 四、验收标准

- 不同学生画像能产生不同排序或不同匹配解释；
- 推荐结果来自真实岗位数据，不能写死岗位名称和分数；
- 每条推荐至少显示岗位名称、匹配分数、匹配技能和缺失技能；
- 岗位描述为空时仍能使用标题或技能字段完成基础推荐；
- 技能为空、岗位为空、筛选后无结果时有明确提示；
- 至少有 3 个测试画像，结果可重复；
- 徐挚凌可以调用推荐函数，王宇杰可以展示返回结果；
- 我本人能解释 TF-IDF、余弦相似度和推荐理由如何产生。

## 五、交给 AI Agent 的任务单

```text
目标：完成一个面向大学生求职的岗位内容推荐模块。
先做：先阅读 AGENTS.md、五人任务分工与验收清单、现有岗位字段和代码，不要猜字段名。
输入：学生画像和清洗后的岗位标题/技能/描述字段。
输出：Top-K 岗位、匹配分数、匹配技能、缺失技能、推荐理由。
限制：只使用现有 Python 依赖；不得连接需要登录的招聘网站；不得写死推荐结果；不得保存个人真实简历或联系方式。
验收：准备 3 个画像；画像变化会改变结果或解释；空技能、空岗位、缺失描述不崩溃；列出修改文件和运行验证。
```

AI Agent 生成后先看 diff，再用三个画像跑一次；如果结果全部相同，要检查文本字段、权重和过滤条件，而不是直接标记完成。

## 六、明天答辩怎么讲

### 一分钟讲解

“我负责个性化岗位推荐。用户输入目标岗位和已有技能后，系统把用户画像与岗位标题、技能、描述转换成 TF-IDF 向量，再用余弦相似度计算相似程度，返回 Top-K 岗位。为了让推荐可解释，我们同时计算用户技能与岗位技能的交集和差集，展示哪些技能已经匹配、哪些技能还需要补充。推荐是辅助决策，不代表录用结果。”

### 可能被问的问题

**问：为什么不用大模型推荐？**

答：课程重点是可解释的数据分析和机器学习。TF-IDF 和余弦相似度计算简单、可复现、便于展示每个特征的作用，适合作为第一版；大模型只作为未来扩展，不作为核心依赖。

**问：余弦相似度是什么？**

答：它比较两个向量方向的接近程度，值越大表示文本中的重要词分布越相似。它不关注文本绝对长度，因此适合比较岗位描述和学生画像的内容匹配。

**问：推荐结果怎么评价？**

答：当前采用固定测试画像、人工检查 Top-K、匹配技能解释和无结果案例作为课程原型评价；如果后续有真实点击或收藏记录，再增加 Precision@K 等行为指标。

## 七、协作接口

- 与郑维豪确定技能字段的分隔符、大小写和空值标记；
- 与徐挚凌确定 `recommend(profile, jobs, top_k)` 的输入输出；
- 与王宇杰确定页面输入控件和推荐卡片字段；
- 与[你的姓名]统一模型输出格式，保证三类算法都能在页面展示。

## 八、不要做的事

不要临时加入向量数据库、深度学习模型或在线大模型 API。当前数据规模下 TF-IDF 已覆盖核心需求；只有在基线推荐运行稳定且确有不足时，才讨论升级。
