# 项目协作规则

这份规则同时给组员和 AI Agent 使用。项目题目确定后，只需要把任务清单里的占位内容替换成实际名称。

## 1. 开工前

- 先阅读本文件和 [《五人任务分工与验收清单》](docs/团队/五人任务分工与验收清单.md)；
- 只做自己负责的任务，不顺手重构、改名或更换技术栈；
- 不确定的需求先写成“待确认”，不要让 AI 自行猜测；
- 修改前先看已有代码，优先复用已有函数和数据结构。

## 2. Git 提交规范

### 分支

- `main` 只放已经可以运行和演示的版本；
- 每人从自己的分支开发：`feat/<姓名>-<任务简称>`；修复问题使用 `fix/<姓名>-<问题简称>`；
- 一项任务完成后再合并，不在 `main` 上直接堆未完成代码。
- 大体积测试数据（如 `tests/fixtures/expanded_jobs.csv`）与 `daily/`、`prompts/` 课程记录必须使用不同分支、不同 PR；仅拆成多个 commit 仍可能造成 PR diff 过大。

### 提交信息

格式：`类型: 简短说明`

允许的类型：

- `feat`：新增功能；
- `fix`：修复问题；
- `docs`：文档或说明；
- `refactor`：不改变功能的整理；
- `test`：测试或验证；
- `chore`：工具、配置等杂项。

示例：

```text
feat: 完成数据清洗流程
fix: 处理空值导致的页面报错
docs: 补充本地运行说明
```

每次提交只完成一件事。不要使用“更新一下”“最终版”“杂项修改”作为提交信息。

### 提交前

```bash
git diff
git status
# 运行本任务最小的检查或演示命令
git add <本次任务涉及的文件>
git commit -m "类型: 简短说明"
```

提交时必须能说明：改了哪些文件、如何验证、还存在哪个问题。未完成的代码不要标成“已完成”。

## 3. 每日过程记录

- 当天完成的开发当天提交，提交前用 `git config user.name` 核对用户名与报名信息一致；不要把提交攒到最后一次性补交。
- 成员目录统一使用学号，不使用姓名拼音，例如 `25051408`。
- 每天从使用的 AI Coding 工具导出当天对话文本，按成员和日期保存到 `prompts/<成员学号>/D当天序号/`，例如 `prompts/25051408/D1/trae.txt`；只提交文本，不提交截图。
- 导出提示词时，必须以前一日提示词最后一条的原始时间戳为下界，扫描全部相关会话归档中的用户消息至当前请求，按原始时间排序；不得只读取当天新建的某一个会话文件。导出文件须记录来源会话、时间范围、条数和去重规则。
- 每天 22:00 前按成员在 `daily/<成员学号>/` 提交当天日报，例如 `daily/25051408/D1.md`。日报严格按飞书模板使用以下栏目；第 1 天没有“昨日遗留问题的回答”一节：
- 组长本人分支的 PR 必须请求丁伟哲（GitHub 用户名 `candleice`）进行第二次 review；review 结果必须留在 PR 中，完成前不合并。

### 篇幅约束（2026-09-03 起）

- `README.md` 不超过 800 字，三节说明项目做什么、给谁使用和技术选型。
- `daily/<学号>/D<n>.md` 不超过 800 字，只写目标、结果、问题和最小验证证据；长命令输出、完整提示词和详细过程放在对应材料中。
- `prompts/<学号>/D<n>/` 全目录合计不超过 10000 字；超过 6 个文件时只保留前 6 个文件用于评审，只保留关键对话，不粘贴大段 AI 输出代码。
- `todos.md` 不限篇幅，但只解析勾选项；每项使用 `- [ ]` 或 `- [x]` 单独占一行。

```markdown
# D1 日报
## 昨日遗留问题的回答
## 今日目标
## 实际进展
## 遇到的问题与解决
## 明日计划
```
- 日报和提示词记录都必须当天完成；课程组的每日反馈问题在次日日报中回答，事后补交不视为当天记录。
- 期末材料统一按学号保存到 `docs/<成员学号>/`，至少包含 `立项报告.md`、`调研报告.md` 和 `项目报告.md`；报告引用的图片放在同目录的 `assets/` 下，并使用相对路径链接。

## 4. AI Agent 使用规则

- AI 生成的代码必须由负责人运行、阅读并能够现场解释；
- 给 AI 的任务要包含：目标、输入、输出、限制和验收方式；
- AI 修改后先查看 `git diff`，确认没有改动任务范围外的文件；
- 不接受没有数据来源、没有运行步骤、没有结果截图或输出的“算法完成”；
- 不把密钥、个人信息、账号 Cookie 放进代码或提交记录；
- 不因 AI 建议就新增依赖、改目录结构或大面积重写，确有必要时先在群里说明；
- 关键提示词、生成代码的修改理由和验证结果保留一份即可，不要求写长日志。

## 薪资口径终态

- 清洗侧以 `salary_text` 为依据，只解析明确的人民币月薪；时薪、日薪、周薪、年薪、计件、面议和单位不明的记录保留原文，`salary_min`、`salary_max`、`salary_avg` 置空，不根据金额或工时猜测单位。
- 算法侧只接收标准化后的 `salary_avg`。算法不得再按“月薪超过 10 万”硬删合法高薪；高薪样本应保留并参与分析，疑似非月薪污染由清洗侧隔离。
- 算法层仅保留 `<500` 元/月的低值异常兜底标记，用于兼容历史污染数据；禁止恢复已删除的 `>100000` 高值阈值。
- 口径验证至少包含：明确高月薪不被标记、非月薪数值列为空、薪资分布计数与有效薪资数一致；真实数据复算脚本和结果保存于 `tests/verify_salary_real_data.py` 与 `tests/fixtures/real_data_salary_verification.json`。

## 5. 统一验收标准

一个任务只有同时满足以下条件，才可以标记为完成：

1. 输入和输出明确，功能能够实际运行；
2. 至少有一个正常案例和一个异常/空数据案例；
3. 负责人能说明核心代码、数据来源和主要取舍；
4. 有最小验证证据：命令输出、页面截图或可重复的演示步骤；
5. 代码已提交到个人分支，且没有混入无关修改；
6. 由组长或指定验收人确认后再合并到 `main`。

## 6. 遇到阻塞

不要为了赶进度伪造数据或把失败结果写成成功。将以下三项发到群里并写入任务清单：

- 已尝试什么；
- 具体错误或缺少什么；
- 下一步需要谁做什么。

如果原方案不可控，优先保留一个可以运行、可以解释的最小版本，并明确未完成部分。
